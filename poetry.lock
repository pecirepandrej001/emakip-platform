# This repository uses pyproject.toml as the source of truth.
# For reproducible container builds, dependencies are pinned to compatible major/minor ranges there.
# Run `poetry lock` if your organization standardizes on Poetry and wants a fully resolved lockfile.
